name: SBOM

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  sbom:
    name: Generate SBOM
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build package
        run: |
          pip install build
          python -m build --sdist --wheel
      - name: Generate SBOM (Python)
        uses: anchore/sbomnix-action@v1
        with:
          path: dist/
          output-file: agentroles-sbom.spdx.json
      - name: Upload SBOM as artifact
        uses: actions/upload-artifact@v4
        with:
          name: sbom
          path: agentroles-sbom.spdx.json
